# 复制此文件为 .env 并填入你的配置

# 方式一：代理到 Cloudflare Worker（推荐，API Key 不上传到本地）
WORKER_URL=https://unlimited-ai.你的用户名.workers.dev

# 方式二：本地直连 NVIDIA API（离线也能跑，需 node server.js direct）
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
